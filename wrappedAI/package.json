{
  "name": "wrappedai",
  "version": "1.0.0",
  "description": "+ switch to the branch and install and build npm    + `npm install`   + `npm run build` --- ## Testing the extension on chrome  + Load the extension from `/wrappedAI`",
  "main": "index.js",
  "scripts": {
    
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/chrome": "^0.1.32"
  }
}
